spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:rms
    username: sa
    password: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop